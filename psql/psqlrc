-- Tab completion in lowercase.
\set COMP_KEYWORD_CASE lower

-- Switch to expanded table format automatically.
\x auto

-- Prompt: user @ database conn_status backend_pid is_in_transaction
\set PROMPT1 '%n@%/%R%(%p)#%x '

\pset null 'Ø'

-- Turn timing on.
\timing
